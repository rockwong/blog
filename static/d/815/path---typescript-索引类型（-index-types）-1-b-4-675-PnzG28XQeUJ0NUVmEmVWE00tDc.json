{"data":{"markdownRemark":{"html":"<h1>索引类型（Index types）</h1>\n<p>js 函数</p>\n<pre><code class=\"language-js\">function pluck(o, names) {\n    return names.map(n => o[n]);\n}\n</code></pre>\n<p>TypeScript 中声明和使用</p>\n<pre><code class=\"language-ts\">function pluck&#x3C;T, K extends keyof T>(o: T, names: K[]): T[K][] {\n  return names.map(n => o[n]);\n}\n\ninterface Person {\n    name: string;\n    age: number;\n}\nlet person: Person = {\n    name: 'Jarid',\n    age: 35\n};\nlet strings: string[] = pluck(person, ['name']); // ok, string[]\n</code></pre>\n<h3>索引类型查询操作符</h3>\n<p> <code>keyof T</code> ,上例为已知公共属性名的联合</p>\n<p><code>let personProps: keyof Person; // 'name' | 'age'</code></p>\n<h3>索引访问操作符</h3>\n<p><code>T[K]</code> 需要确保 <code>K extends keyof T</code></p>\n<pre><code class=\"language-ts\">function getProperty&#x3C;T, K extends keyof T>(o: T, name: K): T[K] {\n    return o[name]; // o[name] is of type T[K]\n}\n</code></pre>","frontmatter":{"title":"索引类型（Index types）"}}},"pageContext":{"slug":"/notes/索引类型（Index types）/","tagPath":"/Typescript/索引类型（Index types）/"}}